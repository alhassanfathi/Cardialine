# Cardialine
Python programme that reads blood sugar levels from a CSV file

## Usage

```bash
python blood_sugar_analysis.py /path/to/readings.csv
```

The CSV can include a `blood_sugar` column (recommended) or single-column numeric values.
